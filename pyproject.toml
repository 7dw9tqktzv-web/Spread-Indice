[project]
name = "spread-indice"
version = "0.1.0"
description = "Spread Trading Indicator — US Index Futures"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
